<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Index</title>
</head>
<body>

  <h1>Handlebars</h1>
  <ul>
    {{#each users as |user|}}
    <div>
      <!-- no need for a pathname on our image src as it has been set in index.js  -->
      <!-- <img src="{{user.username}}_sm.jpg" alt="" />  -->

      <img src="/profilepics/{{user.username}}_sm.jpg" alt="" />
      <a href="/{{user.username}}">{{user.name.full}}</a>
    </div>
    {{/each}}
  </ul>

</body>
</html>
